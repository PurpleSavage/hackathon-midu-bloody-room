
import { section } from "framer-motion/client";
import { motion } from "framer-motion";
export default function SectionModel(){
    return(
        <motion.section
        className="flex flex-col items-center justify-center w-full h-96 bg-black"
        >
            
            
        </motion.section>
    )
}